// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "postgresql"
  url          = "postgresql://neondb_owner:jP1pIfkV8gtc@ep-yellow-cell-a523sqj0.us-east-2.aws.neon.tech/prisma_migrate_shadow_db_57170a52-69bd-491f-a740-ae0a1c172cf4?sslmode=require"
  relationMode = "prisma"
}

model Question {
  id         Int        @id @default(autoincrement())
  title      String
  title_slug String // Add this line
  questionId Int        @unique
  categories Category[] @relation("QuestionCategories")
}

model Category {
  id        Int        @id @default(autoincrement())
  name      String     @unique
  questions Question[] @relation("QuestionCategories")
}
